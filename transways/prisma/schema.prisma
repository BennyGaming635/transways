generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./transways.db"
}

model Stop {
  id        String @id
  name      String
  latitude  Float
  longitude Float
}